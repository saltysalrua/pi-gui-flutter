---
project:
  name: pi-gui
  type: desktop-gui
  frontend: Flutter
  backend: Pi RPC (pi --mode rpc)
  architecture: 前后端分离 (Decoupled Frontend & Backend)
version: 1.0.0
---

# 项目指导规范 (AGENTS.md)

这份文件是参与本项目所有 AI Agent 和开发人员的行动基准。禁止使用虚无缥缈的高深概念，一切以通俗易懂的实用准则为准，明确“怎么做”、“什么时候做”以及“不该做什么”。

```yaml
core_summary:
  architecture: "Flutter 做纯界面与交互，Pi RPC 子进程负责 Agent 逻辑与核心功能，严格前后端分离"
  ui_components: "必须先做原子组件再复用，有新需求先查组件库，尽可能不要新造"
  design_system: "零硬编码：文字全部走 i18n，颜色和字号全部走主题系统，支持主题自适应取色与毛玻璃效果"
  workflow: "做需求先找现实成熟产品参考或直接向人类提问，严禁闭门造车"
  copywriting: "文案必须用大白话，禁止晦涩概念与吹嘘夸张词汇，允许保留标准专业名词"
```

---

## 1. 基础架构：Flutter + Pi RPC（前后端彻底分离）

```yaml
rule_id: ARCH-01
scope: 系统架构与进程通信
roles:
  frontend: "Flutter (桌面客户端 UI、动画、用户交互状态)"
  backend: "Pi RPC (pi --mode rpc 启动的子进程，负责底层模型调度与工具执行)"
```

### 怎么做
* **前端专注于界面**：Flutter 负责所有界面绘制、视觉反馈、用户输入监听和交互动效。
* **后端专注于能力**：通过启动 `pi --mode rpc` 子进程，利用标准输入输出（stdin / stdout）配合 strict JSONL 协议（单行以 `\n` 分隔）进行指令下发和事件流监听。
* **清晰的数据转换层**：在 Flutter 内部建立专门的 RPC 客户端模块（Service / Repository），将收到的原始 JSON 解析映射为强类型的 Dart 实体类，再提供给上层状态管理与组件渲染。

### 什么时候做
* 任何时候只要涉及：读取或管理 Agent 会话、向大模型发送 Prompt、监听工具调用过程（如 bash、edit、read 等）、获取工作区文件变更、切换模型或服务商，**必须全部调用 Pi RPC**。
* 前端只需要向 RPC 派发命令，并静听 RPC 推送回来的事件流更新 UI 状态。

### 不该做什么
* ❌ **绝不在 Flutter 端重写 Agent 逻辑**：禁止在 Flutter 里自行写代码去直接调用大模型 API 或自行实现工具执行引擎。
* ❌ **绝不让 UI 碰原始协议**：UI Widget 绝不能直接接触标准流、原始 JSON 字符串或底层子进程句柄。
* ❌ **绝不破坏前后端隔离**：前端不应越权去直接篡改 Pi 后端私有的运行时内部文件，一切走 RPC 标准协议通道。

---

## 2. UI 开发规范：原子组件先行，杜绝重复造轮子

```yaml
rule_id: UI-01
scope: 组件开发与复用策略
directory: "lib/components/ 或 lib/ui/atoms/"
```

### 怎么做
* **先建零件，再拼整车**：开始搭建大页面前，必须先把最基本的零件做成独立的原子组件（例如通用按钮、输入框、头像、徽章标签、图标按钮、卡片容器、分割线、模态弹窗外壳等）。
* **组件质量要健全**：原子组件必须完整处理自己的各种交互状态（默认、悬停 Hover、聚焦 Focus、禁用 Disabled、加载中 Loading 等）。
* **高复用组装**：任何业务页面必须由这些原子组件拼接而成。

### 什么时候做
* **接到任何新 UI 需求时**：第一件事**必须先去原子组件库里检索**，看是否有现存组件能直接用，或者通过扩展可选参数即可满足。
* **确实无法复用时**：只有当现有组件库彻底无法覆盖需求，且经过充分论证后，才允许新建一个原子组件，并放入公共组件库供全局复用。

### 不该做什么
* ❌ **绝不随手在页面里手写裸组件**：禁止在某个 Page 或 View 内部随便手写一个带有私有装饰样式的 `Container`、裸 `ElevatedButton` 或写死的内联样式。
* ❌ **绝不重复造相同功能的轮子**：禁止每个页面都搞一套自己的私有输入框、私有小按钮或私有卡片。
* ❌ **绝不写不可复用的一次性组件**：写原子组件时禁止硬绑特定的业务数据结构，保持组件的纯粹性与通用性。

---

## 3. 样式与文案：零硬编码、主题自适应与 i18n

```yaml
rule_id: UI-02
scope: 样式系统、国际化与视觉表现
rules:
  text: "禁止任何硬编码中英文字符串，从项目起步就全量接入 i18n"
  color: "禁止硬编码 Hex 颜色，统一调用 Theme 语义 Token，支持系统/壁纸动态取色与自定义配置"
  typography: "禁止硬编码 fontSize 像素值，统一使用排版层级字阶"
  visual_effects: "支持背景虚化与毛玻璃效果 (BackdropFilter 磨砂效果)"
```

### 怎么做
* **文案国际化 (i18n)**：
  * 项目从第一行代码开始引入多语言体系（如 `.arb` 或 JSON 多语言资源）。
  * 页面中的所有文字通过类似 `context.l10n.buttonConfirm` 的方式获取。
* **色彩与主题自适应**：
  * 界面颜色全部绑定主题语义 Token（如 `colorScheme.primary`、`colorScheme.surface`、`colorScheme.onSurface` 等）。
  * 架构设计必须天然支持“自定义主题”与“自适应取色”（例如根据系统壁纸、强调色或用户配置动态生成配色调色板）。
  * 容器和面板样式设计预留透明度与毛玻璃（`BackdropFilter` 磨砂模糊）材质，保证界面质感优雅现代。
* **排版层级**：字号统一从全局 `TextTheme` 获取（如 `bodyMedium`、`titleSmall` 等），禁止写死固定像素大小。

### 什么时候做
* **在编写每一行 UI 代码的当下**：零硬编码是强制红线，绝不允许“先写死文字/颜色，以后再统一抽离”的技术债借口。

### 不该做什么
* ❌ **绝不在代码中出现裸文字**：禁止写 `Text('保存')`、`Text('Settings')`、`Text('确定')` 等硬编码字符串。
* ❌ **绝不在组件里写死色值**：禁止写 `Color(0xFF2196F3)`、`Colors.blue` 等固定颜色。
* ❌ **绝不在组件里写死字号**：禁止写 `TextStyle(fontSize: 14)`。

---

## 4. 需求实现流程：先找参考与确认，禁止闭门造车

```yaml
rule_id: WORKFLOW-01
scope: 需求理解与方案推导
steps:
  1_reference: "找现实参考案例（成熟产品、开源设计、主流 IDE 或 Agent 客户端）"
  2_clarify: "有疑问或不确定时，停下来直接向人类提问确认"
  3_plan: "理清需要用到的原子组件与 RPC 接口交互"
  4_code: "执行编码与自测验证"
```

### 怎么做
* 遇到新功能界面（如侧边栏会话列表、Diff 差异比对器、终端输出容器、多轮对话时间线、工具调用卡片等）：
  1. 先去看看现实中优秀成熟的设计是怎么做的（例如 VSCode、Claude Code 终端/扩展、Codex 桌面端、Cursor 等优秀工具的界面逻辑与排布）。
  2. 善用网络搜索寻找成熟的交互规范与 UI 灵感。
  3. 如果发现需求有不明确的地方、有多种不同方向，**停下来直接问我**，说明选项并向我确认，不要自己猜。
  4. 确认后列出用到的原子组件与 RPC 数据流，然后再动手写代码。

### 什么时候做
* 接到任何新模块构想、重构设计、添加全新交互页面或改变用户操作习惯之前。

### 不该做什么
* ❌ **绝不凭空瞎猜需求**：禁止脱离人类预期自己脑补奇奇怪怪的操作逻辑和页面结构。
* ❌ **绝不闷头盲写**：在核心交互还不明确的时候，严禁直接堆出几百行难以改动的死代码。

---

## 5. 界面文案与提示：大白话、真诚克制，禁止晦涩与吹嘘

```yaml
rule_id: COPY-01
scope: 界面文案、提示语与异常反馈
tone: "大白话、清晰自然、直截了当、平实真诚"
```

### 怎么做
* **用人话写提示**：
  * 按钮文案要直观明了（如“新建会话”、“撤销修改”、“导出记录”）。
  * 状态和提示要通俗易懂，告诉用户当前发生了什么、下一步该点哪里。
  * 报错时直接说人话与解决办法（例如：“网络连接失败，请检查网络设置或稍后重试”，而不是抛出“Socket connection failed: errno 10061”）。
* **保留必要的标准行业名词**：
  * 像 `Git Worktree`、`Diff`、`Token`、`RPC`、`Model`、`Session` 这类大家公认的专业名词无需生硬翻译，可以直接使用，但围绕它们的辅助说明文字必须是浅显直白的大白话。

### 什么时候做
* 编写任何界面标题、按钮标签、输入框占位文字（Placeholder）、悬浮提示（Tooltip）、空状态提示（Empty States）、弹窗确认语和错误提示时。

### 不该做什么
* ❌ **绝不在界面里塞晦涩难懂的学术黑话**：禁止堆砌普通用户根本看不懂的生硬概念。
* ❌ **绝不使用夸张吹嘘词汇**：禁止使用“革命性”、“遥遥领先”、“无与伦比”、“极致颠覆”等浮夸商业吹捧词汇。
* ❌ **绝不出现无解的纯技术报错弹窗**：禁止直接将底层系统未经转换的内部异常堆栈糊在用户脸上。
